# Session handoff — 2026-06-25

Saved before a session restart (GH_TOKEN being added to the environment). Captures
what is not already obvious from the committed files, so the next session starts
with full context.

## Shipped this session (committed to main)

- **Contact enrichment** from Jun 24-25 Gmail/Calendar: Uhlík, Nalewajek, Grindstaff,
  Čáp, Kureková, Omasta; logged first real interaction for Zeníšek.
- **Corrected the Vit Tuhy record** — both his contact file and task-09 wrongly said
  "ball is with Vit / complete." The ball is with Vojta; Vit has chased three times
  (8, 18, 25 Jun) and gone unanswered for 17 days.
- **Reconciled `my-tasks.yaml`** (was 17 days stale): reopened task-09, marked 01/05/10
  complete with evidence, flagged 07/08/11 OVERDUE, 12 still blocked, 06 in_progress.
  Added the rule: mark complete only with evidence, else keep open and tag VERIFY.
- **Added `/reconcile`** (`commands/reconcile.md`) — re-checks open tasks + contact
  follow-ups against the channels, flags contradictions, emits an open-loops view,
  always declaring inputs and as-of time.
- **Wired `gh` into `install.sh`** — installs gh to `~/.local/bin` if missing, uses
  `GH_TOKEN`/`GITHUB_TOKEN`, and dumps project #42 to `~/.claude/data/p42.json` at
  session start (best-effort, non-fatal). `/1on1` and `/weekly-retro` now read #42
  from that file.

## Verify after restart (the point of adding the token)

1. `install.sh` ran and `~/.claude/data/p42.json` exists and is non-empty.
2. If the dump failed, read `~/.claude/data/p42.err`. Most likely cause: the token
   needs **SSO authorisation for the apify org** plus scopes `read:project`,
   `read:org`, `repo`. A fine-grained token without org SSO will 403.
3. Once #42 is flowing, the earlier throughput read can be refreshed (last known:
   Iteration 9 = 29 items / 37 points; trend 95 → 63 → 57 → 37 points).

## Open follow-ups carried forward (offered, awaiting Vojta's go / approval)

- **Vit Tuhy (task-09, live):** draft the Slack reply + pull slots for the joint
  session; onboard his team to Snowflake Intelligence early access. Nothing sent yet.
- **Tableau renewal (task-06):** Fečková (Salesforce) proposed a follow-up call;
  Regina is chasing approval of invoice 38107530 — do not approve until terms settle.
- **New contacts to create:** Regina Dzhuraeva (Senior Financial Controller, owns the
  Tableau invoice), Simona Liptáková (eNPS Product). Tomáš Čuban (Keboola) optional.
- **Tereza:** welcome message + first-week calendar holds were offered, not done.
- **Laura Bočkorová:** touchpoint draft offered, not done.
- All of the above require explicit approval before anything sends.

## Remaining system-improvement backlog (from the session review)

Recommendation 1 (reconcile loop) and 3 (gh access) are done. Still open:

- **Notion task mirror:** "synced manually both directions each session" has already
  drifted. Decide: make the repo YAML the single source of truth and regenerate Notion
  read-only, or drop Notion. Do not leave it manual-bidirectional.
  - Mirror IDs: parent `379f39950a2281d99c48cb9ad1b65189`; Tasks DB
    `399d1cc7a38f4abcbdcfda69457e6ea8`; data source `94380b4a-92a4-4723-af38-8b98a4e76bfc`.
- **`goals.yaml` reconcile:** the `reclaim_phase` success criterion asserts "~40 items /
  ~60 points" but the team is already below it (37 points last sprint). Review is also
  overdue (monthly cadence, last reviewed 2026-05-27).
- **Wire the schedulers:** `schedules.yaml` defines morning briefing / triage / task
  checks with cron, but nothing runs them, and `/enrich stale` is `enabled: false` —
  turn that on (it is the check that would have caught Vit).

## Operational note

Two recurring frictions: the container clock drifted (always verify with
`TZ=Europe/Prague date`), and MCP servers flap (a scan can silently run with Slack or
Snowflake down). Every briefing/scan should state its inputs and as-of time so a
partial run is never mistaken for a complete one — now baked into `/reconcile`.
